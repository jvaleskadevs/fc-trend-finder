// the user FID to send a direct cast with the private report to
export const DC_RECIPIENT_FID = 16628;
// the channelId to send the public report to
export const CHANNEL_RECIPIENT = 'onchain-blocks';
// the maximum casts to get per every user source, max 25
export const CAST_LIMIT_PER_USER = 5;
// the maximum casts to get per every channel source, max 25
export const CAST_LIMIT_PER_CHANNEL = 5;
